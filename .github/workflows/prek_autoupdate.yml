name: prek Autoupdate

on:
  schedule:
    - cron: '19 6 * * *'
  workflow_dispatch:

jobs:
  prek-autoupdate:
    uses: Snuffy2/prek-autoupdate/.github/workflows/prek_autoupdate.yml@v1
    permissions:
      contents: write
      pull-requests: write
      actions: write
    with:
      update-day: "6"
      dispatch-workflows: |
        docs.yml
        linters.yml
        pytest_coverage.yml

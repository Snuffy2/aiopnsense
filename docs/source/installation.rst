Installation
============

Install from `PyPI <https://pypi.org/project/aiopnsense/>`_ in a virtual environment:

.. code-block:: bash

   python -m venv .venv
   source .venv/bin/activate
   pip install aiopnsense

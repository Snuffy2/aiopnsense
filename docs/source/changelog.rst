Changelog
=========

Use GitHub Releases as the canonical release history.

Recommended approach
--------------------

- Publish a GitHub Release for each tagged version.
- Let Read the Docs build a version for each release tag.
- Keep ``latest`` tracking the default branch and ``stable`` tracking the most recent release.

You can later replace this page with an embedded or generated changelog once your release
notes format is standardized.
